# Only needed for `verification-layer sync-db` (writes into internal-db).
# Same value as internal-db/.env.example's DATABASE_URL.
DATABASE_URL=postgresql://hellfire:changeme@localhost:5432/hellfire_internal
